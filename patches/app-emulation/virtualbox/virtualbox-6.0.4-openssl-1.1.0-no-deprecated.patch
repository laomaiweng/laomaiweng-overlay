diff --git a/configure b/configure
index 4f4d8e96..59836248 100755
--- a/configure
+++ b/configure
@@ -836,7 +836,11 @@ check_ssl()
 extern "C" int main(void)
 {
   printf("found version %s", OPENSSL_VERSION_TEXT);
+#if OPENSSL_VERSION_NUMBER < 0x10100000L
   SSL_library_init();
+#else
+  OPENSSL_init_ssl(0, NULL);
+#endif
 #if OPENSSL_VERSION_NUMBER >= 0x10001000
   printf(", OK.\n");
   return 0;
diff --git a/src/VBox/RDP/client-1.8.4/ssl.h b/src/VBox/RDP/client-1.8.4/ssl.h
index 1a034428..13b4d806 100644
--- a/src/VBox/RDP/client-1.8.4/ssl.h
+++ b/src/VBox/RDP/client-1.8.4/ssl.h
@@ -36,6 +36,7 @@
 #include <openssl/bn.h>
 #include <openssl/x509v3.h>
 #include <openssl/hmac.h>
+#include <openssl/rsa.h>
 
 #if defined(OPENSSL_VERSION_NUMBER) && (OPENSSL_VERSION_NUMBER >= 0x0090800f)
 #define D2I_X509_CONST const
diff --git a/src/VBox/RDP/client-1.8.4/tcp.c b/src/VBox/RDP/client-1.8.4/tcp.c
index 326b6c46..7cbf631b 100644
--- a/src/VBox/RDP/client-1.8.4/tcp.c
+++ b/src/VBox/RDP/client-1.8.4/tcp.c
@@ -311,8 +311,12 @@ tcp_tls_connect(void)
 
 	if (!g_ssl_initialized)
 	{
+#if OPENSSL_VERSION_NUMBER < 0x10100000L
 		SSL_load_error_strings();
 		SSL_library_init();
+#else
+		OPENSSL_init_ssl(OPENSSL_INIT_LOAD_SSL_STRINGS, NULL);
+#endif
 		g_ssl_initialized = True;
 	}
 
@@ -325,7 +329,11 @@ tcp_tls_connect(void)
 #  pragma GCC diagnostic ignored "-Wdeprecated-declarations"
 # endif
 #endif
+#if OPENSSL_VERSION_NUMBER < 0x10100000L
 		g_ssl_ctx = SSL_CTX_new(TLSv1_client_method());
+#else
+		g_ssl_ctx = SSL_CTX_new(TLS_client_method());
+#endif
 #ifdef __GNUC__
 # if (__GNUC__ << 8) + __GNUC_MINOR__ >= 0x406
 #  pragma GCC diagnostic pop
diff --git a/src/VBox/Runtime/VBox/VBoxRTDeps.cpp b/src/VBox/Runtime/VBox/VBoxRTDeps.cpp
index 396e0095..7f11bf5d 100644
--- a/src/VBox/Runtime/VBox/VBoxRTDeps.cpp
+++ b/src/VBox/Runtime/VBox/VBoxRTDeps.cpp
@@ -49,6 +49,7 @@
 #include <openssl/rsa.h>
 #include <openssl/ssl.h>
 #include <openssl/rand.h>
+#include <openssl/dh.h>
 
 
 /*********************************************************************************************************************************
diff --git a/src/VBox/Runtime/common/crypto/iprt-openssl.cpp b/src/VBox/Runtime/common/crypto/iprt-openssl.cpp
index 2a0f5297..c42edafd 100644
--- a/src/VBox/Runtime/common/crypto/iprt-openssl.cpp
+++ b/src/VBox/Runtime/common/crypto/iprt-openssl.cpp
@@ -44,9 +44,13 @@ DECLHIDDEN(void) rtCrOpenSslInit(void)
     static bool s_fOssInitalized;
     if (!s_fOssInitalized)
     {
+#if OPENSSL_VERSION_NUMBER < 0x10100000L
         OpenSSL_add_all_algorithms();
         ERR_load_ERR_strings();
         ERR_load_crypto_strings();
+#else
+        OPENSSL_init_crypto(OPENSSL_INIT_LOAD_CRYPTO_STRINGS | OPENSSL_INIT_ADD_ALL_CIPHERS | OPENSSL_INIT_ADD_ALL_DIGESTS, NULL);
+#endif
 
         s_fOssInitalized = true;
     }
diff --git a/src/VBox/Runtime/common/crypto/key-create-rsa-openssl.cpp b/src/VBox/Runtime/common/crypto/key-create-rsa-openssl.cpp
index 0b42a76b..74c47b75 100644
--- a/src/VBox/Runtime/common/crypto/key-create-rsa-openssl.cpp
+++ b/src/VBox/Runtime/common/crypto/key-create-rsa-openssl.cpp
@@ -36,6 +36,7 @@
 # include <iprt/string.h>
 
 # include "internal/iprt-openssl.h"
+# include <openssl/bn.h>
 # include <openssl/rsa.h>
 # include <openssl/err.h>
 
diff --git a/src/VBox/Runtime/common/crypto/ssl-openssl.cpp b/src/VBox/Runtime/common/crypto/ssl-openssl.cpp
index 5326de4e..92f83655 100644
--- a/src/VBox/Runtime/common/crypto/ssl-openssl.cpp
+++ b/src/VBox/Runtime/common/crypto/ssl-openssl.cpp
@@ -87,7 +87,11 @@ RTDECL(int) RTCrSslCreate(PRTCRSSL phSsl, uint32_t fFlags)
     *phSsl = NIL_RTCRSSL;
     AssertReturn(!fFlags, VERR_INVALID_FLAGS);
 
+# if OPENSSL_VERSION_NUMBER >= 0x10100000
+    OPENSSL_init_ssl(OPENSSL_INIT_ADD_ALL_CIPHERS | OPENSSL_INIT_ADD_ALL_DIGESTS, NULL);
+# else
     SSL_library_init();
+# endif
 
     /*
      * We aim at TLSv1 or higher here by default.
